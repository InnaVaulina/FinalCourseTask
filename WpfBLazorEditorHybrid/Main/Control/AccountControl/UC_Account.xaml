<UserControl x:Class="WpfBLazorHybridClient.Main.Control.AccountControl.UC_Account"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfBLazorHybridClient.Main.Control.AccountControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="CadetBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="85*"/>
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0" Grid.Column="2" Background="Transparent" Foreground="White" 
              VerticalAlignment="Center" HorizontalAlignment="Right">
            <MenuItem Header="" />
        </Menu>

        <Menu Grid.Row="1" Grid.Column="0" Background="Transparent" Foreground="White">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Вход" Command="{Binding SelectEntryPage}"/>
            <MenuItem Header="Регистрация" Command="{Binding SelectRegistrationPage}"/>

        </Menu>

        <GridSplitter Grid.Row="1" Grid.Column="1" Background="CadetBlue"
            Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>


        <DockPanel Grid.Row="1" Grid.Column="2" Background="#F0F0F0">
            <ContentControl Content="{Binding Content}" />
        </DockPanel>


    </Grid>
</UserControl>
