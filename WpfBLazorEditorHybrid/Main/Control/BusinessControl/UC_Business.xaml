<UserControl x:Class="WpfBLazorHybridClient.Main.Control.BusinessControl.UC_Business"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfBLazorHybridClient.Main.Control.BusinessControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="CadetBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="85*"/>
        </Grid.ColumnDefinitions>

        
        <Menu Grid.Row="0" Grid.Column="2" Background="Transparent" Foreground="White" 
            VerticalAlignment="Center" HorizontalAlignment="Right">
            <MenuItem Header="Выход" Command="{Binding UserExit}"/>
        </Menu>


        <Menu Grid.Row="1" Grid.Column="0" ItemsSource="{Binding UserFunctions}" Background="Transparent" Foreground="White">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <Menu.Resources>
                <Style TargetType="{x:Type MenuItem}">
                    <Setter Property="Header" Value="{Binding FunctionDisplay}" />
                    <Setter Property="Command" Value="{Binding MenuCommand}" />
                </Style>
            </Menu.Resources>
        </Menu>

        <GridSplitter Grid.Row="1" Grid.Column="1" Background="CadetBlue"
          Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>


        <TabControl Grid.Row="1" Grid.Column="2" Style="{StaticResource mainTabControlStyle}"
          VerticalAlignment="Stretch" HorizontalAlignment="Stretch" 
          TabStripPlacement="Bottom"                                      
          SelectedIndex="{Binding SelectedIndex}"
          ItemsSource="{Binding TabViewModel}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Header}" />


                                <Button BorderThickness="0" Style="{StaticResource ButtonStyle}"
                                      Command="{Binding CloseTab}">
                                    <Border CornerRadius="10" BorderThickness="1" 
                                          BorderBrush="Gray" Margin="10,2,10,2">
                                        <Grid Height="20" Width="20" Style="{StaticResource closeBackground}"/>
                                    </Border>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </TabControl.ItemTemplate>


            <TabControl.ContentTemplate>
                <DataTemplate>
                    <DockPanel>
                        <ContentControl Content="{Binding Content}" />
                    </DockPanel>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>


    </Grid>
</UserControl>
